AWSTemplateFormatVersion: 2010-09-09
Description: Manage Nested Stacks

Parameters:
  StackS3URL:
    Type: String
    Default: https://s3.ap-northeast-1.amazonaws.com/00-cfn-repository

  VPCCidr:
    Type: String
    Default: 10.3.0.0/16

  VPNTunnelCidr:
    Type: String
    Default: 10.8.0.0/24

  DNSServerIp:
    Type: String
    Default: 10.3.255.250

  NTPServerIp:
    Type: String
    Default: 169.254.169.254

  DomainName:
    Type: String
    Default: tuimac.private

  VPNSubnetCidr:
    Type: String
    Default: 10.3.255.240/28

  PrivateSubnetCidr:
    Type: String
    Default: 10.3.0.0/26

  PublicSubnetCidr:
    Type: String
    Default: 10.3.0.64/26

  EndpointSubnetCidr:
    Type: String
    Default: 10.3.0.128/26

  EC2KeyPairName:
    Type: String
    Default: tuimac

Resources:
  VPNNetwork:
    Type: "AWS::CloudFormation::Stack"
    Properties:
      TemplateURL: !Sub
        - ${URL}/network/network.yml
        - { URL: !Ref StackS3URL }
      Parameters:
        VPCCidr: !Ref VPCCidr
        DNSServerIp: !Ref DNSServerIp
        NTPServerIp: !Ref NTPServerIp
        DomainName: !Ref DomainName
        VPNSubnetCidr: !Ref VPNSubnetCidr
        PrivateSubnetCidr: !Ref PrivateSubnetCidr
        PublicSubnetCidr: !Ref PublicSubnetCidr
        EndpointSubnetCidr: !Ref EndpointSubnetCidr

  VPNSecurities:
    Type: "AWS::CloudFormation::Stack"
    Properties:
      TemplateURL: !Sub
        - ${URL}/network/security.yml
        - URL: !Ref StackS3URL
      Parameters:
        VPCId: !GetAtt VPNNetwork.Outputs.VPCId
        PrivateSubnetCidr: !Ref PrivateSubnetCidr
        PublicSubnetCidr: !Ref PublicSubnetCidr
        VPNTunnelCidr: !Ref VPNTunnelCidr

  VPNServer:
    Type: "AWS::CloudFormation::Stack"
    Properties:
      TemplateURL: !Sub
        - ${URL}/vpn/vpn.yml
        - URL: !Ref StackS3URL
      Parameters:
        EC2KeyPairName: !Ref EC2KeyPairName
        VPNSubnetId: !GetAtt VPNNetwork.Outputs.VPNSubnetId
        VPNSecurityGroupId: !GetAtt VPNSecurities.Outputs.VPNSecurityGroupId
        VPNServerIp: !Ref DNSServerIp
        PrivateRouteTableId: !GetAtt VPNNetwork.Outputs.PrivateRouteTableId
        VPNTunnelCidr: !Ref VPNTunnelCidr

  AddRoute:
    Type: "AWS::CloudFormation::Stack"
    Properties:
      TemplateURL: !Sub
        - ${URL}/vpn/addroute.yml
        - URL: !Ref StackS3URL
      Parameters:
        PrivateRouteTableId: !GetAtt VPNNetwork.Outputs.PrivateRouteTableId
        VPNTunnelCidr: !Ref VPNTunnelCidr
        VPNLambdaExecRoleArn: !GetAtt VPNServer.Outputs.VPNLambdaExecRoleArn

Outputs:
  EC2KeyPairName:
    Value: !Ref EC2KeyPairName
    Export:
      Name: EC2KeyPairName
